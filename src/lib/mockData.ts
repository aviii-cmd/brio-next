/**
 * DEPRECATED — All mock data has been removed.
 * Real data comes from Supabase. This file only re-exports
 * types for backward compatibility with any stale import.
 * Do not add mock data here.
 */

// Re-export database types under legacy names for compatibility
export type { Project, Experience, Education, Achievement } from "@/types/database";
